# Kotlin

`foundry-java-kotlin` is an optional layer over `foundry-java-runtime`. Kotlin extensions may improve
ergonomics but must not replace the Java ABI or become a dependency of generated Java code.
